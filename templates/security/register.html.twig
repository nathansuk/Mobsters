{% extends 'base.html.twig' %}

{% block title %} {{ sitename }} : Inscription {% endblock %}

{% block body %}

    {% for message in app.flashes('notice') %}
        <h1 style="color: red;">{{ message }}</h1>
    {% endfor %}

    {{ form_start(register_form) }}
    <h1>Inscription</h1>

    <h4>Pour s'inscrire, il faut que tu renseignes ton pseudo sur city !
    Ensuite, sur city, tu dois changer ta mission en "CODE-IM-Pseudo" en remplaçant "Pseudo" par ton pseudo sur city !
    </h4>
    {{ form_widget(register_form) }}
    <button type="submit" class="btn btn-primary">M'inscrire</button>
    {{ form_end(register_form) }}

{% endblock %}